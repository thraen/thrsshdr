#version 400
layout (location = 0) in vec3 pos;

uniform double E[400];

uniform int _elapsed_t;
float sc;

void main(){ 
// 	sc =   0.5+0.2*sin(float(_elapsed_t)/4000)
// 	     + 0.5
// 		 + 0.5*sin( 0.02*(float(E[1]) + float(E[2]) + float(E[3]))/200 );

// 	sc = 1.2*sc;
	sc = 0.7;

// 	sc = 0.9+0.5*sin( 0.02*(float(E[1]) + float(E[2]) + float(E[3]))/200 );

	gl_Position = vec4(sc*pos.x, sc*pos.y, pos.z, 1.0);
}
